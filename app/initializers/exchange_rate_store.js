import ExchangeRateStore from "cartera/store/exchange_rate";

export default {
    name: "exchange_rate_store",
    initialize: function (container, application) {
        // application.register("store:main", ExchangeRateStore);
        // application.inject("route", "store", "store:main");
        // application.inject("controller", "store", "store:main");
    }
};
